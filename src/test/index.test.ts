export {}

describe("index", () => {
  it("should pass", async () => {
    /**
     * We don't have any unit tests because the code executes a few commands
     * and then ends.
     *
     * It's not really possible to test except by using it.
     *
     * Never the less, we include a unit test here so that we can keep the
     * repo consistent with the other npm repos which use the presets.
     */
  })
})
